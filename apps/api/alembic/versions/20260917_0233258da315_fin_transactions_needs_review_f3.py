"""fin_transactions.needs_review: очередь разбора (Ф3)

Одна колонка по `SPEC.md` §15.3 и §15.6. Имя файла на латинице намеренно:
оно строится из сообщения `-m` средствами консоли, и на Windows русский текст
приезжает в cp1251 - то же решение, что в ревизиях e5220d372bfa, f0021f38eb6f
и a7c3f81b6e42.

**Зачем колонка, если есть пустая категория.** §15.6 требует очередь
«Требует внимания» вне сальдо, а выразить её нечем: `category_id` пуст
у всех операций до Ф4, и очередь разбора совпала бы со всей книжкой. Роль
счёта - про счёт, а не про операцию: у одного счёта операций сотни, и
в разбор из-за неразмеченного счёта уходит счёт, а не каждая его строка.

**Заводится импортом в одном случае** - неоднозначный кандидат на дозревание
(§15.3): ожидающая операция книжки и строка файла совпали по дате, карте
и описанию, но кандидатов с какой-то стороны больше одного. Выбор между
«дозрело» и «вторая настоящая покупка» стоит денег, поэтому оба конца
уходят в разбор и остаются вне сальдо, пока owner не решит.

**Индекса нет намеренно.** Значение `true` - редкое исключение, и запрос
сальдо фильтрует по `false`, то есть по почти всей таблице: индекс там
не применится. Очередь разбора короткая по определению - её незачем
индексировать, пока она не перестанет быть короткой.

Revision ID: 0233258da315
Revises: a7c3f81b6e42
Create Date: 2026-09-17 16:44:28.946584+00:00
"""

from collections.abc import Sequence

import sqlalchemy as sa
from alembic import op

revision: str = "0233258da315"
down_revision: str | None = "a7c3f81b6e42"
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None


def upgrade() -> None:
    op.add_column(
        "fin_transactions",
        sa.Column(
            "needs_review",
            sa.Boolean(),
            server_default=sa.text("false"),
            nullable=False,
        ),
    )


def downgrade() -> None:
    op.drop_column("fin_transactions", "needs_review")
