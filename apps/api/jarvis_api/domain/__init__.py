"""Предметная логика без HTTP (Э6).

Здесь то, что обязано быть проверено без `TestClient`: границы недели по зоне
owner, покрытие зеркала расписания, группировка событий по дням, конфликты,
правило пометки давности. Функции получают готовую `Session` первым аргументом
и не коммитят - тот же раздел обязанностей, что в джобах (`jobs/sync_itmo.py`):
границу транзакции ставит вызывающий код.

Инвариант 1: backend - источник истины. Правило «что считается конфликтом»
и границы недели живут здесь, а не на клиенте, потому что зависят от
`settings.timezone`, которой у клиента нет.
"""
