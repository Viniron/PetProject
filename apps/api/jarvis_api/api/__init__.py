"""Граница HTTP: роутеры, зависимости, формат ошибок, схемы контракта (Э6).

Слой тонкий намеренно. Здесь живёт разбор параметров, перевод отказов в коды
и сборка ответа - вся арифметика (границы недели, покрытие зеркала, конфликты,
давность) лежит в `jarvis_api.domain` и проверяется без `TestClient`.

Каталог называется `api`, а не `routers`, потому что кроме роутеров тут
зависимости, обработчики исключений и схемы: имя `routers` врало бы про
содержимое. `CLAUDE.md` перечисляет слои, и `api` - имя слоя.
"""
