import type { Курс } from "@/shell/tabs";

/**
 * Установленные курсы.
 *
 * Их ноль - это рабочее состояние, а не заглушка (CLAUDE.md): первый релиз
 * идёт без курсов (ADR-019), и капсула с одной вкладкой «Calendar» - ровно
 * то, что видит owner сегодня. Эндпоинта списка курсов в контракте нет
 * и придумывать его фронт не имеет права: он появится вместе с движком
 * курсов, и тогда изменится тело этой функции, а не устройство капсулы.
 */
export function установленныеКурсы(): readonly Курс[] {
  return [];
}
