/**
 * Экран календаря - SPEC §8.4.
 *
 * Каркасный шаг Э7 отрисовывает заголовок и оболочку; сетка недели, полоса
 * давности и три вида пустоты приезжают следующим шагом вместе с вызовом
 * `GET /api/calendar`. Выдуманной сетки здесь нет намеренно: показать
 * правдоподобные пары вместо настоящих - прямое нарушение инварианта 9.
 */
export default function CalendarPage() {
  return (
    <div className="content-col">
      <h1 className="screen-title">Calendar</h1>
      <p className="screen-sub">Расписание ИТМО и события</p>
      <div className="slot">
        <b>Сетка недели</b>
        появится на следующем шаге этапа - вместе с запросом к API
      </div>
    </div>
  );
}
