/**
 * Настройки - вкладка вне генерируемого списка курсов (этап 1 дизайна).
 *
 * Экран пуст не по недосмотру: всё, что сегодня настраивается, живёт
 * в `.env` на плате и в таблице `settings`, а трогать их через интерфейс
 * никто не просил. Вкладка существует, потому что капсула утверждена
 * с ней; наполнение придёт, когда появится первая настройка, которую
 * owner меняет чаще, чем раз в полгода.
 */
export default function SettingsPage() {
  return (
    <div className="content-col">
      <h1 className="screen-title">Settings</h1>
      <p className="screen-sub">Пока нечего настраивать</p>
      <div className="slot">
        <b>Настроек нет</b>
        часовой пояс, окно забора расписания и ключи живут на плате
      </div>
    </div>
  );
}
