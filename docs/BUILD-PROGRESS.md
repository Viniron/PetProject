# Реализация — состояние

**Текущий этап: Э1 — хост: Pi, Postgres, туннель, бэкап (в работе)**
**Последнее обновление: 2026-09-03 (вечер, конец сессии)**
**Продолжать с раздела «Э1 · где мы остановились» в конце файла**

---

## Протокол — читать первым

Claude в любом чате по этому проекту, **перед любой работой по коду**:

1. **Прочитать этот файл** → взять текущий этап из строки выше.
2. Сказать owner, что делается и на какой пункт `SPEC.md` это опирается.
3. Сделать этап по плану, одной веткой и одним коммитом.
4. **Прогнать проверку** (Definition of Done из `CLAUDE.md`) и показать owner,
   что именно проверено — списком, а не общими словами.
5. **Ждать «ок».** Без «ок» этап не закрывается.
6. После «ок»: отметить этап утверждённым в таблице, дописать решения
   в «Решения по ходу», перейти к следующему и обновить строку
   «Текущий этап» и дату.

Отличие от `design/PROGRESS.md` одно и существенное: **«ок» без зелёной
проверки этап не закрывает.** Дизайн оценивается глазами, код — прогоном.

Этот файл — **единственный источник истины** по тому, где мы находимся
в реализации. Не спрашивать owner «на чём мы остановились» — ответ здесь.
Состояние проектирования интерфейса — в `design/PROGRESS.md`, это
отдельная дорожка со своим протоколом.

---

## Опора

План релиза — ADR-019 в `docs/DECISIONS.md`, **с поправкой ADR-020**:
весь стек сразу на Pi, бесплатного этапа нет. Границы продукта, инварианты
и Definition of Done — `CLAUDE.md`. Поведение — `SPEC.md`.

Цель релиза: **работающий календарь без курсов на домашнем Raspberry Pi** —
API, база и фронт на своём железе с первого дня, с офсайт-бэкапом, который
переживает смерть носителя.

---

## Этапы

| # | Этап | Опора в SPEC | Статус |
|---|------|--------------|--------|
| Э0 | Скелет монорепо | — | ✅ утверждён 2026-09-03 |
| Э1 | Хост: Pi, Postgres, туннель, бэкап | §11.1 | 🔄 в работе с 2026-09-03 |
| Э2 | Схема БД v1 | §9, §11.2 | ⬜ не начат |
| Э3 | Интеграция my.itmo.ru | §10 | ⬜ не начат |
| Э4 | Интеграция Google Calendar | §10 | ⬜ не начат |
| Э5 | Джобы и планировщик | §11.2 | ⬜ не начат |
| Э6 | API календаря | §8.4, §10 | ⬜ не начат |
| Э7 | Фронт | §8.1, §8.4 | ⬜ не начат |
| Э8 | Слой моделей и захват | §5 | ⬜ не начат |
| Э9 | Эксплуатация и живучесть | §11.1, §11.2 | ⬜ не начат |

Статусы: ⬜ не начат · 🔄 в работе · ✅ утверждён

Число этапов не догма. Если этап распадается на два — таблица
перенумеровывается, она для этого и нужна.

**Э1 стоит перед схемой БД не случайно.** Managed-база бралась у провайдера
за пять минут, свой хост поднимается руками. Дешевле сделать это до первой
миграции, чем после интеграций: OS и SSD, Docker, Postgres в compose,
Cloudflare Tunnel, ночной `pg_dump` в Backblaze B2 с ping в healthchecks
и **однократное восстановление из скачанного дампа, проделанное owner
руками**. Последнее и есть критерий закрытия этапа — не «скрипт написан».

**Э9 — не «деплой», деплой случился на Э1.** Это живучесть: автоподъём,
проверенный выдёргиванием питания, а не чтением compose-файла; UPS;
heartbeat приложения; restore-тест джобом раз в месяц; проверка catch-up
после суточного простоя (§11.2).

---

## Что нужно от owner и когда

Секретные значения в чат присылать не нужно: owner вставляет их сам
в `.env` на Pi — панели переменных окружения у чужого хостера больше нет
(ADR-020). В чат — только несекретное (почта для расшаривания календарей,
id проекта, имя бакета).
Пошаговую инструкцию по каждому сервису Claude даёт на своём этапе.

| # | Сервис | Что получится | Переменная | Этап | Статус |
|---|--------|---------------|------------|------|--------|
| 1 | Pi 4/5, USB-SSD, UPS | Pi 4 (2 ГБ) поднят, **носитель microSD** | — | Э1 | ✅ с оговоркой |
| 2 | Cloudflare: аккаунт и домен | туннель, HTTPS-имя | `CF_TUNNEL_TOKEN` | **Э7** | ⬜ отложено |
| 3 | Backblaze B2 | бакет + ключ **только на запись** | `B2_*` | Э1 | ⬜ |
| 4 | сторож бэкапа (UptimeRobot / Better Stack) | ping-URL бэкапа и джобов | `HEARTBEAT_URL` | Э1 | ⬜ |
| 5 | ИСУ | логин и пароль | `ISU_LOGIN`, `ISU_PASSWORD` | Э3 | ⬜ |
| 6 | Google Cloud + service account | JSON-ключ | `GOOGLE_SA_JSON` | Э4 | ⬜ |
| 7 | Google AI Studio + Anthropic | два API-ключа | `GEMINI_API_KEY`, `ANTHROPIC_API_KEY` | Э8 | ⬜ |

Вычеркнуты по ADR-020: managed Postgres, бесплатный хост API, Vercel,
cron-job.org с его `JOBS_TICK_SECRET`. База своя, тик не нужен, фронт на Pi.

`DATABASE_URL` теперь не приходит от провайдера, а складывается на Э1 из
пароля, который owner задаёт сам. `ISU_CRED_KEY` нигде не регистрируется —
owner генерирует его сам и хранит в двух местах. Потеря = повторный ввод
пароля ИСУ. Утечка вместе с дампом базы = компрометация пароля; поскольку
дампы уезжают в B2 нешифрованными, ключ в бакете оказаться не должен
никогда — он живёт только в `.env` на Pi и у owner.

---

## Инварианты хоста

Соблюдаются с первого коммита. Нарушение любого из семи — блокер этапа,
а не замечание.

Раздел раньше назывался «инварианты переезда» и оправдывался будущим
переносом с бесплатного хоста. Переезда больше нет (ADR-020), но пункты
держатся на собственных основаниях — теперь на восстановлении из бэкапа,
а не на переносе. Проверочный вопрос к каждому: **выживет ли это при
замене умершего SSD на новый.**

1. **Всё состояние только в Postgres,** включая сырьё открытых черновиков
   (`bytea`). На диск контейнера приложение не пишет ничего. Причина сменилась,
   но не ослабла: `pg_dump` — единственная дверь, через которую данные попадают
   в бэкап. Файл, записанный мимо базы, в копию не войдёт и восстановлен не будет.
2. **Конфиг только через env.** Платформенных API хостера на Pi нет, и заводить
   самодельные аналоги в файлах на диске нельзя — см. пункт 1.
3. **Схема только через Alembic.** Единственный способ повторить структуру базы
   на чистом хосте перед заливкой дампа.
4. **Один `docker-compose.yml`,** различия окружений — в overlay и env.
   Образ multi-arch: `linux/arm64` для Pi, `linux/amd64` для машины owner и CI.
5. **`external_key` детерминирован от данных источника** и не содержит ни id
   строки БД, ни имени хоста, ни времени генерации. Главная защита от порчи
   данных, и проверяется буквально: восстановление из дампа не должно задвоить
   расписание при первом же reconcile.
6. **Prod-база на Pi, dev-база отдельно.** Тесты и миграции в prod не бьют:
   единственная копия данных — та, что в бэкапе, и терять её на отладке нечем
   оправдаться. Разделение обеспечивается разными `DATABASE_URL`, а не аккуратностью.
7. **Время tz-aware, в БД UTC.** Дамп, залитый на хост с другой локалью, —
   обыденный способ сдвинуть всё расписание на час.

---

## Среда разработки — проверено 2026-08-28

Разведка сделана заранее, чтобы Э0 начинался с работы, а не с выяснения.
Проверять заново не нужно; изменилось — исправить строку здесь.

| Что | Состояние | Как вызывается |
|---|---|---|
| Python 3.12 | **есть**, 3.12.10 (обновлён с 3.12.3 на Э0) | `py -3.12` (голый `python` — это 3.13 из Microsoft Store) |
| Node | **есть**, v24.19.0 | `node`, `npm` |
| git | есть, remote `github.com/Viniron/PetProject`, `gh` авторизован | — |
| `make` | **есть**, GnuWin32 3.81, поставлен на Э0 | `make`; путь в PATH дописан вручную — winget этого не делает |
| Docker | Docker Desktop, поставлен на Э0 | `docker compose` |

**Установка окружения, проверена и работает:**

```bash
cd apps/api
py -3.12 -m venv .venv
./.venv/Scripts/python.exe -m pip install -e ".[dev]"
```

`.venv/` уже закрыт `.gitignore` (строка 61), отдельного правила не нужно.
На Э0 это же делает цель `make venv`.

**Бывшая поломка `mypy`: вылечена на Э0, диагноз разведки был неверным.**
`python -m mypy` падал с `ImportError: DLL load failed while importing internal`,
и разведка списала это на сборку Python из Microsoft Store. Проверка показала
другое: `py -3.12` и раньше указывал не на Store, а на python.org
(`AppData/Local/Programs/Python/Python312`), дело было в патч-версии.
Обновление 3.12.3 → 3.12.10 и пересоздание venv починили всё: `mypy --strict`
проходит локально, обходные пути не понадобились.

Вывод шире одного инструмента: прежде чем обходить поломку окружения, стоит
обновить компонент, на который она списана. Обход стоил бы этапу постоянной
дыры в проверке типов.

---

## Э0 · Скелет монорепо — техническое задание

Цель: `test` и `lint` существуют и зелёные **до** первой строки бизнес-логики.
Ничего предметного в этом этапе нет — ни календаря, ни ИТМО, ни моделей.

**Состав.**

```
apps/api/
  pyproject.toml            зависимости, ruff + black + mypy + pytest в одном файле
  jarvis_api/
    __init__.py             версия пакета
    config.py               Settings на pydantic-settings, всё из env
    main.py                 FastAPI app + GET /health
  tests/
    test_health.py          живость + tz-aware время
Makefile                    dev / test / lint / migrate / sync-itmo / plan-today / backup
make.ps1                    те же цели для Windows, пока make не установлен
.env.example                только заглушки
infra/docker-compose.yml    api + postgres, restart: unless-stopped
infra/Dockerfile.api        multi-arch: linux/amd64 + linux/arm64
.github/workflows/ci.yml    lint + mypy --strict + pytest на Linux
.pre-commit-config.yaml     gitleaks (публичный репозиторий, ADR-019)
```

**Что уже выяснено и не нужно переоткрывать:**

- `fastapi` ставится версии 0.141; `TestClient` тянет `httpx` и предупреждает
  `StarletteDeprecationWarning: install httpx2 instead` — предупреждение
  безвредное, но `httpx2` в dev-зависимостях снимет шум.
- Набор правил ruff, который стоит взять: `E, F, W, I, UP, B, DTZ, ASYNC, T20`.
  **`DTZ` здесь не украшение** — он превращает инвариант 7 (нет наивных
  `datetime`) из пункта ревью в ошибку линтера. `T20` ловит `print` в сервисе.
- `/health` **не должен трогать базу**: он обязан отвечать и до того, как
  `DATABASE_URL` заполнен, иначе нечем проверять поднятие стека на Э1.
- Тесты пишутся по-русски в именах — так же, как комментарии в `tokens.css`.

**Решения, которые нужно принять на Э0 (спросить owner, не выбирать молча):**

1. **`make`.** Либо owner ставит его (`winget install GnuWin32.Make` или
   Git Bash + `choco install make`), либо в репозитории живёт `make.ps1`
   с теми же целями. `CLAUDE.md` требует `Makefile` — он будет в любом случае,
   вопрос только в том, чем owner запускает цели у себя.
2. **Docker.** На Pi обязателен — там весь стек. На машине owner нужен раньше,
   чем предполагалось: dev-база больше не берётся у managed-провайдера
   (ADR-020). Два варианта, выбирает owner: **(а)** Docker Desktop на Windows
   и Postgres контейнером локально — работает без домашней сети и физически
   не может задеть prod; **(б)** второй инстанс `jarvis_dev` на Pi, доступный
   по локальной сети, — Docker Desktop тогда не нужен вовсе и 3–4 ГБ RAM
   на ноутбуке остаются свободными, но разработка вне дома требует туннеля,
   а prod рядом. Вариант (а) безопаснее, (б) дешевле.
3. **`apps/web`.** Скелет Next.js создаётся `create-next-app` и приносит
   сотни файлов. Предложение: в Э0 фронта нет вообще, он целиком в Э7 —
   иначе Э0 перестаёт быть проверяемым за один взгляд.
4. **`mypy`** — см. поломку выше: в CI обязательно, локально по возможности.

**Критерий закрытия Э0:**

- `pytest` зелёный (2 теста: живость, tz-aware);
- `ruff check` и `black --check` чисты;
- `mypy --strict` чист в CI;
- `docker compose config` валиден (без запуска);
- `gitleaks` в pre-commit срабатывает на подложенном тестовом секрете;
- `.env.example` содержит **только** заглушки, ни одного настоящего значения.

---

## Решения по ходу

Сюда дописывается всё, что owner поправил или выбрал, и всё, что найдено
проверкой. Строка на решение, с номером этапа. Это защита от повторного
обсуждения одного и того же в новом чате.

**До начала.** Пакет решений, с которого стартовал релиз, — ADR-019:
бесплатный хост как временный этап, managed Postgres, service account
вместо OAuth, принятый риск с кредами ИСУ, публичный код, захват в трёх
модальностях, сырьё живёт до подтверждения или отмены, назначение моделей.

**2026-09-02 · пересмотр плана, ADR-020.** Owner решил ставить базу сразу
на Pi. Отменено: бесплатный хост, managed Postgres, Vercel, cron-job.org
вместе с внешним тиком и эндпоинтом `/internal/jobs/tick`, этап репетиции
переезда. Добавлено: этап Э1 «Хост» перед схемой БД; бэкап в Backblaze B2
как подсистема с проверенным восстановлением; фронт статическим экспортом
на том же Pi. Дампы не шифруются — решение owner, риск очерчен в ADR-020;
взамен ключ B2 выдаётся только на запись, чтобы сбой или компрометация Pi
не стёрли историю копий, а ретеншн живёт правилами на стороне облака.

**Э0.** Разведка среды сделана отдельно от реализации: `make` и Docker на
машине owner отсутствуют, `mypy` не запускается из-за Store-сборки Python.
Ни одно из трёх не блокирует Э0, но каждое требует решения owner — они
перечислены в ТЗ выше. Пробный скелет был написан и прогнан (тесты и линтеры
зелёные), затем удалён: этап делается целиком и в свою очередь, а не
наполовину в чужом чате.

**Э0 · решения owner, 2026-09-02.**

- **`make` ставится** (`winget install GnuWin32.Make`; вторым сообщением owner
  поручил установку Claude). Следствие: `make.ps1`,
  заявленный в ТЗ, **не создаётся**. Вторая копия списка целей расходится с
  первой ровно тогда, когда про неё забывают, а держать её незачем, если
  `make` есть у owner, на Pi и в CI.
- **dev-база — Docker Desktop на Windows.** Второй инстанс `jarvis_dev` на Pi
  отклонён: локальный контейнер физически не может задеть prod и работает вне
  дома без туннеля. Это закрывает вопрос, оставленный открытым в ADR-020.
- **Фронта в Э0 нет,** `apps/web` целиком на Э7.
- **Python обновляется ради `mypy`.** Решение формулировалось как «поставить
  python.org вместо Store-сборки»; при исполнении выяснилось, что Store-сборкой
  `py -3.12` никогда и не был, и хватило обновления патч-версии — подробности
  ниже, в разделе про среду.

**Э0 · найдено проверкой.**

- `httpx2` существует (2.12.0) и **заменяет** `httpx` в dev-зависимостях, а не
  дополняет: `starlette.testclient` перестаёт предупреждать совсем.
- `mypy` по каталогу зашёл бы в `.venv` и проверял чужие пакеты `--strict`.
  В цели `lint` пути перечислены явно; `ruff` и `black` этой проблемы не имеют,
  потому что уважают `.gitignore`.
- Добавлен `.gitattributes` с `eol=lf`, которого не было в ТЗ. Причина конкретная:
  `Makefile` с CRLF в Linux не работает — make видит возврат каретки в конце рецепта и не
  находит команду. Правится один раз здесь, а не разбирательством в CI.
- Заглушки целей `migrate`, `sync-itmo`, `plan-today`, `backup` выходят с кодом 1,
  а не печатают сообщение и делают вид, что успешны.
- В compose добавлено явное `name: jarvis`. Без него имя проекта берётся из
  имени каталога, и контейнеры зовутся `infra-api-1`. На Pi это имя попадёт
  в команды восстановления и в логи, и гадать по нему не должно приходиться.
- Проверено, что без `POSTGRES_PASSWORD` компоуз падает с кодом 1, а не
  подставляет пустую строку: дефолтного пароля у базы нет и не появится молча.
- Версии GitHub Actions подняты до `checkout@v7` и `setup-python@v7` — на v4/v5
  CI предупреждал о снятом с поддержки Node.js 20.

**Э0 · окружение доведено до рабочего состояния** (owner: «всё, что нужно,
установи сам»). Поставлено: GnuWin32 make 3.81, Python 3.12.10 вместо 3.12.3,
Docker Desktop, **WSL2**. Две неочевидности, которые стоит помнить при
переустановке машины:

- winget-пакет `GnuWin32.Make` **не прописывает make в PATH** — путь
  `C:/Program Files (x86)/GnuWin32/bin` дописан в пользовательский PATH руками,
  без этого цели не находятся.
- Docker Desktop на Windows 11 Home **не запускается без WSL2**: Hyper-V на
  Home-редакции недоступен, других бэкендов нет. `wsl --install` требует прав
  администратора, иначе молча повторяет своё же сообщение «подсистема не
  установлена». После установки WSL хватило перезапуска Docker Desktop,
  перезагрузка Windows не понадобилась — демон поднялся за 18 секунд.

**Э0 · что проверено, чем именно.**

| Пункт критерия | Как проверен |
|---|---|
| `pytest` | 2/2 зелёные, локально и в CI |
| `ruff check`, `black --check` | чисты, через `make lint` |
| `mypy --strict` | чист локально и в CI |
| `docker compose config` | зелёный локально через `make compose-check` и в CI |
| `.env.example` только заглушки | глазами и `grep`: все значения `CHANGE_ME` |
| `gitleaks` в pre-commit | подложен фейковый `ghp_…`: правило `github-pat`, `leaks found: 1`, код 1 |

Сверх критерия проверено: `/health` отвечает `200` под настоящим uvicorn,
а не только под `TestClient`; правила `DTZ` и `T20` действительно ловят
наивный `datetime` и `print` — подложенный файл дал два срабатывания и код 1;
без `POSTGRES_PASSWORD` компоуз падает с кодом 1, а не подставляет пустую
строку.

Инфраструктура проверена не только валидацией файлов — раз Docker появился
на машине, дешевле было поднять её целиком, чем оставить Э1 разбираться
с ошибками Э0:

- образ по `infra/Dockerfile.api` собирается **и под `linux/amd64`, и под
  `linux/arm64`** одним файлом — заявленная multi-arch не осталась комментарием;
- контейнер отвечает на `/health` и работает под непривилегированным
  `uid=10001(jarvis)`;
- `docker compose up` поднимает связку целиком: база становится `healthy`,
  api ждёт её по `depends_on: service_healthy` и тоже становится `healthy`;
- `/health` через проброшенный порт вернул `env: prod` — значит `${ENV:-prod}`
  из compose дошёл до `Settings`, и конфиг через env работает насквозь
  (инвариант хоста 2), а не только в тестах.

Смысл этих проверок не в галочках: `/health` под настоящим uvicorn даёт
уверенность в контейнере, а `DTZ` превращает инвариант 7 в исполняемое
правило, а не в договорённость.

CI прогнан на PR #1 (`feat/e0-skeleton` → `main`), оба джоба зелёные.

**Э0 закрыт 2026-09-03.** «Ок» owner получен после зелёной проверки, а не
вместо неё: все шесть пунктов критерия прогнаны, четыре из них — дважды,
локально и в CI.

**2026-09-03 · Э1 отложен по решению owner.** Состояние входных данных на эту
дату: Pi куплен, ОС на нём не ставилась; Cloudflare, Backblaze B2 и
healthchecks.io не заведены. Owner попросил не начинать Э1 до отдельного
сигнала — **не начинать этап и не «готовить его заранее»**, даже той частью,
что от железа не зависит.

**Э1 · блок снят, решения owner, 2026-09-03.** Owner дал тот самый отдельный
сигнал («снимаю блок, начинаем») и закрыл три развилки до первой строки кода.

- **Ночной бэкап планирует systemd timer на хосте,** а не cron внутри
  контейнера. Юниты лежат в `infra/systemd/` и ставятся симлинком; таймер
  вызывает `docker compose run --rm backup --apply`. Причина: логи уходят
  в journald, расписание видно в `systemctl list-timers`, ручной прогон —
  одна команда, и cron в контейнере не приходится примирять с
  непривилегированным пользователем. Цена решения названа: часть настройки
  живёт на хосте, а не целиком в compose.
- **На Э1 в prod-базе живёт таблица-проба `restore_probe`** — несколько строк
  с tz-aware временем, создаётся скриптом. Без неё восстановление проверяет
  только тракт «дамп скачался, `pg_restore` прошёл»; с ней — что данные на
  месте, а это и есть смысл бэкапа. Удаляется на Э2 первой миграцией.
  Инвариант «схема только через Alembic» не затронут: это проба бэкапа,
  а не схема приложения.
- **SSH-доступ к Pi даётся Claude.** Owner ставит ОС, дальше Docker, клон,
  compose, туннель и прогоны делает Claude по SSH — тем же порядком, каким
  на Э0 ставились make и Docker. Секреты в `.env` вписывает owner сам:
  в чат они не попадают ни при какой схеме доступа.

**Э1 · хост поднят, 2026-09-03.** Плата работает, стек живёт, автоподъём проверен.

Железо и система, как есть:

- **Pi 4 Model B Rev 1.1, 2 ГБ RAM** (1844 МБ видит система), 4 ядра, swap 1,8 ГБ.
  Расход стека: API 37 МБ + база 23 МБ, свободно 1,4 ГБ. Запас есть, но сборку
  образа на самой плате держать под наблюдением.
- **Носитель — microSD 32 ГБ, а не USB-SSD.** Отступление от `SPEC.md` §11.1,
  принятое сознательно: SSD у owner есть, но внутренний ноутбучный, а USB-бокса
  к нему нет. Переезд на SSD дешевле проделать пока база пустая — задача
  остаётся открытой и от неё зависит цена бэкапа: карта расходник, и
  единственной защитой истории занятий пока остаётся офсайт-копия.
- **ОС переустановлена, и это был блокер, а не гигиена.** На карте стояло
  32-битное Raspberry Pi OS (Bullseye, образ 2022-09-22); под 32-битный ARM
  официальные образы Postgres не выпускаются, а `infra/Dockerfile.api` собран
  под `linux/arm64`. Поставлено Raspberry Pi OS Lite 64-bit — Debian 13 trixie,
  образ 2026-06-18, `aarch64`. Разрядность старой системы опознаётся по
  `config.txt` без `arm_64bit=1` и по четырём ядрам на bootfs.
- **Docker CE 29.7.2 + Compose v5.5.0** из официального репозитория Docker для
  trixie (он существует, проверено). Пакет `docker.io` из Debian 13 — 26.1.5,
  заметно старее, поэтому не брался.

Находки, каждая из которых стоила бы отдельного разбирательства в новом чате:

- **Raspberry Pi Imager 2.x провижионит через cloud-init,** а не через
  `custom.toml` или `firstrun.sh`: в командной строке ядра появляется
  `ds=nocloud;i=rpi-imager-<id>`, на bootfs лежат `user-data`, `meta-data`,
  `network-config`. Положенный руками `custom.toml` не читается вовсе — он так
  и остался на разделе нетронутым, вместе с паролем Wi-Fi открытым текстом
  (удалён). Проверять, применились ли настройки, надо по cloud-init файлам.
- **memory cgroup отключён прошивкой платы,** и в `cmdline.txt` этого не видно:
  `cgroup_disable=memory` добавляет firmware. Без него Docker не ограничивает
  память контейнеров. Дописано `cgroup_enable=memory cgroup_memory=1`; проверено
  фактом, а не файлом — контейнер с `--memory 64m` получил ровно 67108864 байт.
- **`docker compose` ищет `.env` в каталоге compose-файла,** то есть в `infra/`,
  а не в корне репозитория, где живёт `.env.example`. Сейчас стек поднимается
  с `--env-file .env`; в `Makefile` это надо внести на Э1a, иначе `make` на Pi
  свалится на отсутствии `POSTGRES_PASSWORD` — причём не сразу, а на первой
  цели, которая дойдёт до compose.
- **Беспарольного `sudo` cloud-init не выдаёт,** в отличие от старых образов с
  пользователем `pi`. Настроен `/etc/sudoers.d/010_jarvis-nopasswd`.
- **Локальная сеть с ноутбука была недоступна из-за kill-switch в AmneziaWG:**
  клиент блокирует всё, что не идёт в туннель, включая домашние адреса. Симптом,
  по которому это узнаётся сразу: `ping` до собственного роутера отвечает
  `General failure`, при живом интернете через VPN. Лечится снятием галочки
  «Block untunneled traffic» — выключать VPN целиком не требуется.

Проверено на плате:

| Что | Как проверено |
|---|---|
| `/health` | `200` за 5 мс, `env: prod` — значит `.env` доходит до `Settings` насквозь |
| Postgres | 17.11 `aarch64`, `show timezone` = UTC (инвариант 7 на живой базе) |
| Автоподъём §11.1 | `systemctl reboot`, ни одной команды на запуск — через 42 с оба контейнера `healthy`, `/health` = 200 |
| Ограничение памяти | контейнер с `--memory 64m` получил ровно 64 МБ |
| Docker в автозапуске | `systemctl is-enabled docker containerd` — оба `enabled` |

**Э1 · решения по ходу, 2026-09-03.**

- **`sudo` без пароля для `jarvis`.** Обоснование: вход на плату только по
  ключу (аутентификация по паролю в SSH отключена при провижининге), наружу
  плата не выставлена, и беспарольный `sudo` — штатное поведение Raspberry Pi OS
  для первого пользователя. Альтернатива — передавать пароль в каждой команде,
  оставляя его в списке процессов, что хуже. Файл ставил owner руками: пароль
  знает только он, и в чат он не попадал.
- **Пароль базы сгенерирован на плате** (28 символов) и лежит в `~/jarvis/.env`
  с правами 600. В репозиторий и в чат не попадает; `DATABASE_URL` собирается
  из `POSTGRES_*` внутри compose, второй копии пароля не существует.
- **Доступ Claude к плате** — ключ `~/.ssh/jarvis_pi` на машине owner,
  пользователь `jarvis`. **Обращаться по имени `jarvis.local`, а не по адресу:**
  адрес выдаёт DHCP, и он меняется при смене сети — за один день 2026-09-03
  сменился с `192.168.0.111` на `192.168.0.109` после переезда платы. Имя
  отвечает по mDNS в любой сети. Если mDNS молчит, плата ищется сканированием
  подсети по MAC-префиксу `dc:a6:32`.

Осталось в Э1: `cloudflared` (нужен токен), выгрузка в B2 (нужен бакет и ключ
только на запись), ping в healthchecks, код Э1a (overlay, джоб бэкапа с тестами,
restore, systemd timer, правка `Makefile`, `RUNBOOK`), таблица-проба
`restore_probe` и восстановление, проделанное owner руками.

**Э1a · код бэкапа написан и проверен на плате, 2026-09-03.**

Что появилось:

| Файл | Роль |
|---|---|
| `apps/api/jarvis_api/integrations/b2.py` | клиент B2 на стандартной библиотеке: три запроса нативного API |
| `apps/api/jarvis_api/jobs/backup.py` | джоб: `pg_dump -Fc`, sha1, выгрузка, ping, ретеншн |
| `apps/api/tests/test_backup.py` | 16 тестов; всего в прогоне 18 |
| `infra/Dockerfile.backup` | образ на `postgres:17-alpine` - `pg_dump` той же сборки, что сервер |
| `infra/docker-compose.pi.yml` | overlay: туннель (в профиле) и бэкап, том `db-backups` |
| `infra/systemd/jarvis-backup.{service,timer}` | расписание, `Persistent=true` |
| `infra/restore-check.sh` + `restore-check-inner.sh` | разворачивание свежего дампа в отдельную базу |
| `infra/restore-probe.sql` | проба Э1, удаляется первой миграцией Э2 |
| `docs/RUNBOOK.md` | поднять хост с нуля, завести сервисы, восстановиться |

Почему клиент B2 на стдлибе, а не S3-совместимый: S3-шлюз B2 требует boto3
или awscli - десятки мегабайт в образе, который делает один PUT в сутки.
Нативный API - `b2_authorize_account`, `b2_get_upload_url`, `b2_upload_file`,
и ни одной новой зависимости (CLAUDE.md запрещает их без спроса).

Проверено на живой плате, не только тестами:

| Что | Как |
|---|---|
| dry-run | дамп снят (2007 байт), sha1 посчитан, наружу ноль байт, код 0 |
| сквозной тракт | подставной B2 в сети compose: `authorize` -> `get_upload_url` -> `upload` (имя `daily/...`, 2007 байт, **sha1 совпал**) -> `heartbeat`. Порядок ADR-020 проверен фактом, а не чтением кода |
| отказ выгрузки | B2 погашен: код возврата **1**, запись «бэкап не выполнен», и лог подставного сервера **не изменился** - ping не ушёл |
| нехватка настроек | `--apply` без ключей: код **1**, сообщение перечисляет все четыре переменные сразу |
| восстановление | `make restore-check` развернул дамп в `jarvis_restore`: таблица на месте, **3 строки пробы** - вернулись данные, а не только схема |
| таймер | установлен симлинками, включён, следующий запуск 04.09 03:30 MSK |
| падение видно systemd | ручной прогон при незаполненном B2: `failed`, `Result=exit-code`, `ExecMainStatus=1`, причина в журнале |
| линтеры и типы | `make lint` чист (ruff, black, `mypy --strict`), `make test` 18/18, `make compose-check` на обоих файлах |

Находки Э1a, каждая стоила бы часа в новом чате:

- **`.gitignore` прятал два файла инфраструктуры.** Правила `*.sql` и
  `*.backup` защищают от коммита дампов, и под них по совпадению расширения
  попали `infra/Dockerfile.backup` и `infra/restore-probe.sql`: лежат на
  диске, а `git status` их не видит. Добавлены явные исключения - не
  `git add -f`, потому что про флаг забывают, и файл теряется при следующем
  клоне на чистую плату.
- **`${VAR:?...}` в compose стреляет до профилей.** Обязательный
  `CF_TUNNEL_TOKEN` ронял любую команду с overlay - вплоть до сборки образа
  бэкапа, - пока Cloudflare не заведён. Туннель убран в профиль `tunnel`,
  включаемый строкой `COMPOSE_PROFILES=tunnel` в `.env`, то есть там же и
  тогда же, где появляется сам токен. CI намеренно проверяет overlay **без**
  этой переменной - иначе возврат `:?` пройдёт незамеченным.
- **`COPY` переносит права из контекста сборки.** Каталоги, приехавшие с
  Windows по scp, дали внутри образа `700` у root, и процесс под `postgres`
  не смог импортировать пакет. Проявилось это как `No module named
  jarvis_api.jobs.backup`, то есть как отсутствие кода, а не как отказ
  доступа. В `Dockerfile.backup` добавлен `chmod -R a+rX /app`.
- **`heartbeat_url` обязателен при `--apply`** - решение, а не недосмотр:
  бэкап без контроля это бэкап, о поломке которого узнают в день
  восстановления. Заставляет завести healthchecks до первой реальной выгрузки,
  ровно как требует ADR-020.

**Э1 · туннель отложен до Э7, решение owner, 2026-09-03.** Owner спросил,
за что в проекте придётся платить, и по итогам разговора перенёс доступ извне
на этап фронта.

Существо решения: **сам Cloudflare Tunnel бесплатен**, платный только домен
(~200 ₽/год за `.ru`, ~$10 за `.com`) - без него постоянного HTTPS-имени не
бывает, а бесплатные адреса `trycloudflare` меняются при каждом перезапуске.
Но доступ извне сейчас не даёт ничего: открывать нечего, интерфейс появится
на Э7. Значит платёж, единственный в проекте до этапа моделей, можно не
делать сегодня, а решение о нём принять, когда будет что открывать.

**ADR-017 не отменяется** - способ доступа остаётся тем же, сдвигается срок.
Рассматривалась и замена на Tailscale Funnel (публичный HTTPS без своего
домена, бесплатно); owner её не выбрал, и правка ADR не понадобилась.

Код от этого не страдает: сервис `cloudflared` уже описан в overlay и лежит
в профиле `tunnel`, включаемом строкой `COMPOSE_PROFILES=tunnel` в `.env`
одновременно с появлением токена. Инструкция по заведению туннеля написана
и ждёт в `docs/RUNBOOK.md`.

Осталось для закрытия Э1 - всё за owner:

1. Backblaze B2: бакет, ключ **без `deleteFiles`**, правила жизненного цикла.
2. healthchecks.io: чек и ping-URL.
3. Первый реальный `make backup-apply` и зелёный чек в healthchecks.
4. **Восстановление из скачанного из B2 дампа, проделанное руками** - это и
   есть критерий закрытия этапа (ADR-020), «скрипт написан» его не закрывает.

Туннель в критерий закрытия Э1 больше не входит - он переехал в Э7 вместе
с фронтом, которому и нужен.

**Э1 · сервис контроля бэкапа сменён, решение owner, 2026-09-03.**
На healthchecks.io регистрация у owner не прошла. Взамен - UptimeRobot или
Better Stack: те же heartbeat-мониторы в бесплатном плане.

Кода это не касается совсем: джоб делает GET по `HEARTBEAT_URL`, и любой
приёмник, понимающий GET, подходит. ADR-020 не правится - в нём назван
принцип (ping после успешной выгрузки, сторож снаружи), а healthchecks.io
был лишь примером.

Рассматривался и более сильный вариант, отклонённый owner: сторож на GitHub
Actions, который раз в сутки заглядывает в бакет и проверяет **наличие
свежего дампа**, а не отчёт джоба о себе. Он не требует ни одной новой
регистрации и переживает смерть платы, но требует второго ключа B2 на чтение
в GitHub Secrets и правки ADR-020. Если регистрация не пойдёт и здесь -
возвращаемся к нему.

Требование к сервису остаётся одно и оно не про удобство: **сторож живёт вне
дома.** Монитор на самой плате промолчит ровно в том случае, ради которого
затевался, - когда плата умерла.

**Э1 · первый реальный бэкап выполнен, 2026-09-03.** Дамп ушёл в живой бакет
`Vinironjarvis`: B2 вернул настоящий `fileId`, сторож Better Stack ответил
`200`. Тракт «дамп - выгрузка - ping» работает не на подставном сервере,
а на реальных сервисах.

**Э1 · «Write Only» у Backblaze - это не только запись.** Проверка фактических
прав ключа (через `b2_authorize_account`, права приходят в ответе) дала:

```
deleteFiles, listBuckets, writeBucketEncryption, writeBucketLifecycleRules,
writeBucketLogging, writeBucketNotifications, writeBucketReplications, writeFiles
```

Два следствия, оба существенные:

- **`deleteFiles` присутствует** - прямое противоречие ADR-020, где записано,
  что скомпрометированная или сбойнувшая плата физически не должна уметь
  стереть историю копий. Это единственная защита от сценария «ошибка в
  скрипте или взлом снесли и данные, и бэкапы».
- **`listFiles` отсутствует** - перечислить содержимое бакета этим ключом
  нельзя. Удалять вправе, посмотреть не вправе.

В веб-интерфейсе нужного набора не собрать: там три готовых варианта прав.
Поштучно они задаются только через API, поэтому написан `infra/b2-create-key.py`
- спрашивает мастер-ключ (нигде не сохраняется, живёт в памяти процесса),
создаёт ключ с правами ровно `writeFiles` и `listFiles`, вписывает его в `.env`
сам и предлагает отозвать старый. Скрипт отказывается работать в
неинтерактивной оболочке: в ней он молча ждал бы ввода - проверено тем, что
именно это и произошло при первой попытке запустить его из другого скрипта.

---

## Э1 · где мы остановились

Состояние на конец сессии 2026-09-03. **Читать это первым при продолжении.**

Работает и проверено: хост, стек, автоподъём, бэкап целиком (включая живой B2
и сторожа), восстановление из локального дампа, таймер systemd.

**Дальше по порядку.**

Сделано после первой записи этого раздела: ключ B2 перевыпущен (права
`writeFiles`, `listFiles`, без удаления), раскладка копий по префиксам
`daily`/`weekly`/`monthly` реализована и проверена на плате, тестов 24,
переезд платы в другую сеть проделан и описан в RUNBOOK.

Осталось - всё за owner:

1. **Удалить старый ключ B2.** В App Keys два ключа с именем `jarvis-backup`;
   удалить тот, у которого в правах есть `deleteFiles`. Пока он жив, право
   стирать историю копий существует.
2. **Выставить правила жизненного цикла бакета** (Bucket Settings ->
   Lifecycle): `daily/` скрывать через 8 дней и удалять через 1;
   `weekly/` - 29 и 1; `monthly/` - 190 и 1. Прав менять эти правила у ключа
   на плате намеренно нет: право сократить ретеншн - это право уничтожить
   историю.
3. **Зафиксировать код в git** - см. список файлов ниже.
4. **Восстановление руками из скачанного из B2 дампа.** Критерий закрытия Э1
   (ADR-020). Процедура - `docs/RUNBOOK.md`, раздел «Восстановление из бэкапа».

**Не сделано и не входит в Э1:** туннель Cloudflare (отложен до Э7 решением
owner), переезд с microSD на USB-SSD (нужен USB-бокс, отступление от SPEC
§11.1 зафиксировано выше).

**Код Э1 в git не зафиксирован.** Owner коммитит сам; на 2026-09-03 в рабочем
дереве машины owner лежат незакоммиченными: `integrations/`, `jobs/`,
`tests/test_backup.py`, `docs/RUNBOOK.md`, `infra/Dockerfile.backup`,
`infra/docker-compose.pi.yml`, `infra/restore-check.sh`,
`infra/restore-check-inner.sh`, `infra/restore-probe.sql`, `infra/systemd/`,
`infra/b2-create-key.py` плюс правки `.env.example`, `.github/workflows/ci.yml`,
`.gitignore`, `Makefile`, `config.py`, `pyproject.toml`.

**На плате те же файлы лежат копией, а не из git** (доставлены по scp, потому
что коммита ещё не было). После коммита на плате нужно
`cd ~/jarvis && git checkout -- . && git pull`, иначе рабочая копия и
репозиторий будут расходиться молча.

**Э1 · плата переехала в другую сеть, 2026-09-03.** Owner перенёс Pi и
подключил ноутбук к другой точке доступа. Плата при этом знала одну сеть -
ту, что прописал Imager, - и в новом месте в сеть не вышла вовсе.

Процедура переезда придумана, проделана и записана в `docs/RUNBOOK.md`
(раздел «Плата переехала в другую сеть»). Существо: профили NetworkManager
лежат в `/etc/NetworkManager/system-connections/`, то есть в ext4, которого
Windows не видит, **но** у Raspberry Pi OS есть штатный способ выполнить
скрипт при загрузке, задаваемый с FAT32-раздела - тот же `systemd.run` в
`cmdline.txt`, которым пользуется Imager для своего `firstrun.sh`. Скрипт
создаёт профиль, убирает себя из `cmdline.txt`, удаляется с раздела (иначе
на FAT32 остался бы лежать пароль Wi-Fi) и перезагружает плату.

Проверено на живой плате: `jarvis.local` отвечает в новой сети, скрипт себя
убрал, `cgroup_enable=memory` не потерялся, стек поднялся сам, `/health` -
200, бэкап с нового места выгрузился в B2 и получил `200` от сторожа.

**Прежняя сеть при этом сохранилась:** `nmcli` показывает оба профиля -
`TP-Link_BC6F_5G` (приоритет 10) и `netplan-wlan0-IRINA_5G` (приоритет 0).
То есть плата теперь знает обе сети и следующий переезд между ними
переживёт сама, без карты.

**Э1 · у платы нет часов, и это влияет на бэкап.** Найдено по расхождению:
`docker ps` показывал контейнеры «Up 2 hours» при uptime системы 8 минут.
Разгадка - у Raspberry Pi нет батарейки RTC: при включении время берётся из
последнего сохранённого systemd и уточняется по NTP только после появления
сети. Docker записал время старта по неверным часам, потом они прыгнули на
2,5 часа вперёд.

Для бэкапа это не косметика: **имена дампов строятся из времени, а ретеншн
сортирует копии по имени.** Дамп, снятый до синхронизации, получил бы имя
«из прошлого» и был бы удалён первым - при том, что он самый свежий.
Лечение - две строки в `infra/systemd/jarvis-backup.service`:

```
Wants=time-sync.target
After=time-sync.target
```

Теперь джоб не стартует раньше, чем часы выверены. Проверено:
`systemctl show jarvis-backup.service -p After` содержит `time-sync.target`.

Сортировку по имени менять на сортировку по времени файла не стали намеренно:
`mtime` зависит от тех же часов, и проблему это не решило бы, а порядок
перестал бы читаться глазами.

**Э1 · ключ B2 перевыпущен, 2026-09-03.** Новый ключ создан скриптом
`infra/b2-create-key.py` и имеет ровно `writeFiles` и `listFiles`, ограничен
бакетом `Vinironjarvis`; `deleteFiles` нет - требование ADR-020 выполнено.
Проверено не логом скрипта, а обращением к B2: права приходят в ответе
`b2_authorize_account`.

Две вещи, найденные при этом:

- **`input()` с русским промптом валит скрипт** на плате:
  `UnicodeDecodeError: 'utf-8' codec can't decode byte 0xd0`. `readline`
  декодирует приглашение по локали, и хотя `print` тем же текстом работает,
  `input` падает. Из-за этого ответ `y` не дошёл до кода удаления, и старый
  ключ остался жив. Все приглашения ко вводу переведены на латиницу, причина
  записана в шапке скрипта - чтобы это не «починили» обратно.
- **Справка о длинах ключей была неверной.** У мастер-ключа `keyID` - это
  идентификатор аккаунта, около 12 символов, а сам ключ около 42; числа
  25/31 относятся к обычным ключам. Первый `401` owner получил именно из-за
  этой путаницы.

**Э1 · раскладка копий по префиксам, 2026-09-03.** Требование ADR-020 про
глубину 7 ежедневных / 4 недельных / 6 месячных не выполнялось: джоб кладёт
дамп в один префикс, а правила жизненного цикла B2 различают копии
**только по префиксу имени**. Значит раскладывать обязан джоб - иначе все
копии попадают под одно правило и живут одинаково недолго.

Сделано: `target_prefixes()` возвращает `daily` всегда, добавляет `weekly`
в воскресенье (конец недели по ISO - недельной копией становится последняя
за неделю, а не первая) и `monthly` первого числа. `upload_dump()` грузит
файл под каждым префиксом, авторизуясь в B2 один раз: токен живёт сутки.

Тестов стало 24 (было 18). Добавлены четыре случая раскладки, включая тот,
который ломается тихо: **первое число, выпавшее на воскресенье** - копия
обязана лечь трижды, а не дважды (проверено на 2026-11-01). Плюс проверка,
что авторизация одна на все префиксы.

Проверено на живой плате:

| Что | Как |
|---|---|
| три префикса | прогон с датой 2026-11-01 на подставном B2: `daily/`, `weekly/`, `monthly/` - три выгрузки, ping после всех |
| обычный день | живой прогон 2026-09-03 (четверг): только `daily/`, как и должно |
| ретеншн | впервые сработал на живых данных: восьмая локальная копия удалена, осталось семь |
| содержимое бакета | спрошено у облака (право `listFiles` появилось вместе с новым ключом): три копии, у каждой сходится sha1 |

**Числа для правил жизненного цикла B2**, которые остаётся выставить owner
в настройках бакета:

| Префикс | Скрыть через | Удалить через | Смысл |
|---|---|---|---|
| `daily/` | 8 дней | 1 день | 7 ежедневных копий плюс запас на сутки |
| `weekly/` | 29 дней | 1 день | 4 недельных |
| `monthly/` | 190 дней | 1 день | 6 месячных |

Прав менять эти правила у ключа на плате намеренно нет: право сократить
ретеншн - это право уничтожить историю.
