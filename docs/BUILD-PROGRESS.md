# Реализация — состояние

**Текущий этап: Э0 — Скелет монорепо**
**Последнее обновление: 2026-08-28**

---

## Протокол — читать первым

Claude в любом чате по этому проекту, **перед любой работой по коду**:

1. **Прочитать этот файл** → взять текущий этап из строки выше.
2. Сказать owner, что делается и на какой пункт `SPEC.md` это опирается.
3. Сделать этап по плану, одной веткой и одним коммитом.
4. **Прогнать проверку** (Definition of Done из `CLAUDE.md`) и показать owner,
   что именно проверено — списком, а не общими словами.
5. **Ждать «ок».** Без «ок» этап не закрывается.
6. После «ок»: отметить этап утверждённым в таблице, дописать решения
   в «Решения по ходу», перейти к следующему и обновить строку
   «Текущий этап» и дату.

Отличие от `design/PROGRESS.md` одно и существенное: **«ок» без зелёной
проверки этап не закрывает.** Дизайн оценивается глазами, код — прогоном.

Этот файл — **единственный источник истины** по тому, где мы находимся
в реализации. Не спрашивать owner «на чём мы остановились» — ответ здесь.
Состояние проектирования интерфейса — в `design/PROGRESS.md`, это
отдельная дорожка со своим протоколом.

---

## Опора

План релиза целиком — ADR-019 в `docs/DECISIONS.md`. Границы продукта,
инварианты и Definition of Done — `CLAUDE.md`. Поведение — `SPEC.md`.

Цель релиза: **работающий календарь без курсов**, сначала на бесплатном
хостинге, затем перенос на Raspberry Pi вместе с базой и без потери данных.

---

## Этапы

| # | Этап | Опора в SPEC | Статус |
|---|------|--------------|--------|
| Э0 | Скелет монорепо | — | 🔄 в работе |
| Э1 | Схема БД v1 | §9, §11.2 | ⬜ не начат |
| Э2 | Интеграция my.itmo.ru | §10 | ⬜ не начат |
| Э3 | Интеграция Google Calendar | §10 | ⬜ не начат |
| Э4 | Джобы и планировщик | §11 | ⬜ не начат |
| Э5 | API календаря | §8.4, §10 | ⬜ не начат |
| Э6 | Фронт | §8.1, §8.4 | ⬜ не начат |
| Э7 | Слой моделей и захват | §5 | ⬜ не начат |
| Э8 | Деплой | §11.1 | ⬜ не начат |
| Э9 | Репетиция переезда | §11.1 | ⬜ не начат |

Статусы: ⬜ не начат · 🔄 в работе · ✅ утверждён

Число этапов не догма. Если этап распадается на два — таблица
перенумеровывается, она для этого и нужна.

---

## Что нужно от owner и когда

Секретные значения в чат присылать не нужно: owner вставляет их сам
в `.env` и в панель переменных окружения хостинга. В чат — только
несекретное (почта для расшаривания календарей, id проекта, регион).
Пошаговую инструкцию по каждому сервису Claude даёт на своём этапе.

| # | Сервис | Что получится | Переменная | Этап | Статус |
|---|--------|---------------|------------|------|--------|
| 1 | Google Cloud + service account | JSON-ключ | `GOOGLE_SA_JSON` | Э3 | ⬜ |
| 2 | Managed Postgres (Neon/Supabase) | строка подключения | `DATABASE_URL` | Э1 | ⬜ |
| 3 | Free-хост API | публичный URL | — | Э8 | ⬜ |
| 4 | Vercel / Cloudflare Pages | адрес сайта | — | Э8 | ⬜ |
| 5 | ИСУ | логин и пароль | `ISU_LOGIN`, `ISU_PASSWORD` | Э2 | ⬜ |
| 6 | Google AI Studio + Anthropic | два API-ключа | `GEMINI_API_KEY`, `ANTHROPIC_API_KEY` | Э7 | ⬜ |
| 7 | cron-job.org | внешний триггер джобов | `JOBS_TICK_SECRET` | Э4 | ⬜ |
| 8 | healthchecks.io | ping-URL | `HEARTBEAT_URL` | Э4 | ⬜ |
| 9 | Офсайт-бэкап (S3) | ключи бакета | `BACKUP_*` | Э8 | ⬜ |
| 10 | Pi 4/5, USB-SSD, UPS, Cloudflare | железо и туннель | — | после Э9 | ⬜ |

`ISU_CRED_KEY` нигде не регистрируется — owner генерирует его сам и хранит
в двух местах. Потеря = повторный ввод пароля ИСУ. Утечка вместе с дампом
базы = компрометация пароля.

---

## Инварианты переезда

Соблюдаются с первого коммита, иначе переезд на Pi станет дорогим.
Нарушение любого из семи — блокер этапа, а не замечание.

1. **Всё состояние только в Postgres,** включая сырьё открытых черновиков
   (`bytea`). На диск контейнера приложение не пишет ничего: у free-хостов
   диск эфемерный, любой файл на нём — будущая потеря.
2. **Конфиг только через env.** Никаких платформенных API хостера
   (Vercel KV, Render Disks, Fly Volumes) — на Pi их не будет.
3. **Схема только через Alembic.**
4. **Один `docker-compose.yml`** на обоих хостах, различия в overlay и env.
   Образ multi-arch: `linux/amd64` + `linux/arm64`, Pi это arm64.
5. **`external_key` детерминирован от данных источника** и не содержит ни id
   строки БД, ни имени хоста, ни времени генерации. Главная защита от порчи
   данных: хостозависимый ключ — и первый reconcile на Pi задвоит расписание.
6. **Секреты переносятся значениями.** Другой `ISU_CRED_KEY` — и шифротекст
   пароля не расшифруется; другой service account — и календари чужие.
7. **Время tz-aware, в БД UTC.** Хосты в разных зонах — обыденный способ
   сдвинуть всё расписание на переезде.

---

## Решения по ходу

Сюда дописывается всё, что owner поправил или выбрал, и всё, что найдено
проверкой. Строка на решение, с номером этапа. Это защита от повторного
обсуждения одного и того же в новом чате.

**До начала.** Пакет решений, с которого стартовал релиз, — ADR-019:
бесплатный хост как временный этап, managed Postgres, service account
вместо OAuth, принятый риск с кредами ИСУ, публичный код, захват в трёх
модальностях, сырьё живёт до подтверждения или отмены, назначение моделей.
